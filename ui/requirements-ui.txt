# Dependencies for the Bryan run launcher UI (ui/).
#
# Deliberately SEPARATE from the repo's requirements.txt and _env_bryan.yml.
# The UI runs in its own environment and shells out to Bryan's interpreter, so
# the frozen bryan29 conda env used to reproduce study results is never touched.
# See ui/README.md.

nicegui>=3.14
pandas>=2.2
openpyxl>=3.1
psutil>=5.9

# The Lake record page's catchment rainfall (core/awap.py): the AWAP / AWRA-L
# grids are netCDF, the catchment a shapefile, reprojected if it is not in
# latitude and longitude. Imported only when the rainfall is made.
netCDF4>=1.6
pyshp>=2.3
pyproj>=3.6

# tests
pytest>=8.0
pytest-asyncio>=0.23      # ui/tests/test_results_page.py renders a page
